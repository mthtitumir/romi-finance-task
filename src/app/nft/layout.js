
export const metadata = {
    title: 'NFT Mint | Decentralize Perpetual Exchange',
  }
const NftLayout = ({children}) => {
  return (
    <div>
        {children}
    </div>
  )
}

export default NftLayout